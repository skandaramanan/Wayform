node_modules/
*.log
.env
.DS_Store
# Local clone of the shared context repo (never commit into the server repo)
.context-store/
# Per-user hook config (identity + context repo URL) — never commit
.memorylayer-hook.env
# Per-user Claude Code settings (personal permissions live here, NOT in the shared settings.json)
.claude/settings.local.json
# Agent authoring-workflow scaffolding (task briefs, reports, diffs) — not product; decisions live in the MemoryLayer store + docs/
.superpowers/
docs/onboarding/self-onboarding.md
docs/
.cursor/mcp.json
.cursor/settings.json

# Codex project-scoped MCP config — regenerated per-member by `wayform init --remote`
.codex/config.toml
.gstack/
docs/onboarding/pilot-onboarding.local.md
